package pt.isel.ls.services;

import pt.isel.ls.utils.CommandResult;

public class GetTopHandler extends Handler implements IHandler {

    @Override
    public CommandResult execute() {
        return null;
    }
}
